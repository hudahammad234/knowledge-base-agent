# Requirements — Member 1 (Document Processing & Embeddings)
# Covers: metadata.py, loader.py, chunker.py, embeddings.py

# -- loader.py --
pypdf              # reading PDF files
python-docx        # reading DOCX files (imported as `docx`)
pandas             # reading CSV files

# -- chunker.py --
langchain-text-splitters   # RecursiveCharacterTextSplitter

# -- embeddings.py (default local provider) --
langchain-huggingface       # HuggingFaceEmbeddings wrapper
sentence-transformers       # underlying all-MiniLM-L6-v2 model

# -- embeddings.py (optional Gemini provider, only if EMBEDDING_PROVIDER=gemini) --
langchain-google-genai
